DATA SEGMENT
	NUM1 DW 8884H
	NUM2 DW 4444H
	NUM3 DD 12345678H
	AM DD ?
	AD DD ?
DATA ENDS

CODE SEGMENT
	ASSUME DS:DATA, CS:CODE

START:	MOV AX, DATA
		MOV DS, AX
		
		MOV AX, NUM1
		MUL NUM2
		LEA BX, AM
		MOV [BX],AX
		MOV [BX+2],DX
		
		LEA BX, NUM3
		MOV AX,[BX]
		MOV DX,[BX+2]
		DIV NUM1
		LEA BX, AD
		MOV [BX],AX
		MOV [BX+2],DX
		
		MOV AH, 4CH
		INT 21H
CODE ENDS
	END START
