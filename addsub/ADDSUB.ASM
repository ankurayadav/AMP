MYDATA SEGMENT
	NUM1 DW 10H
	NUM2 DW 20H
	AADD DW ?
	ASUB DW ?
	MYCONST EQU 05H
MYDATA ENDS

MYCODE SEGMENT
		ASSUME DS:MYDATA, CS:MYCODE
START:	MOV AX, MYDATA
		MOV DS, AX
	
		MOV BX, NUM1
		ADD BX, NUM2
		MOV AADD, BX
		
		MOV BX, NUM2
		SUB BX, NUM1
		MOV ASUB, BX
		
		MOV BX, ASUB
		SUB BX, MYCONST
		MOV ASUB, BX
		
		MOV AH, 4CH 
		INT 21H
MYCODE ENDS
	END START