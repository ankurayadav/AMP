DATA SEGMENT
	ERRMSG DB "DIV BY ZERO OCCURRED FGHFGHFG"
	LENSTR EQU $-ERRMSG
DATA ENDS

STK SEGMENT STACK
	DW 50 DUP(0)
	TOS LABEL WORD
STK ENDS

PUBLIC ERRMSG
PUBLIC LENSTR
EXTRN DIVBY0:FAR

CODE SEGMENT
	ASSUME CS:CODE, DS:DATA, SS:STK

START:	MOV AX, DATA
	MOV DS, AX
	MOV AX, STK
	MOV SS, AX
	LEA SP, TOS
	
	MOV AX, 00
	MOV ES, AX
	
	MOV WORD PTR ES:[0000], OFFSET DIVBY0
	MOV WORD PTR ES:[0002], SEG DIVBY0
	
	MOV AX, 1000
	MOV CL,02
	DIV CL

	
	MOV AH, 4CH
	INT 21H
	
	
	CODE ENDS

END START 